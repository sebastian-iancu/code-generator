{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q 'composer\\.lock$' && echo '{\"decision\":\"block\",\"reason\":\"Do not edit composer.lock manually — use Composer commands instead.\"}' || true",
            "statusMessage": "Checking for protected files..."
          }
        ]
      }
    ]
  }
}
